# 节点备注功能更新说明

## 更新内容

已对 SimpleMindMap 插件的节点备注功能进行了以下改进：

### 1. 右侧备注面板
- 备注现在显示在右侧面板中，而不是浮窗在节点位置
- 面板默认宽度为 300px，可调整大小

### 2. 面板调整大小功能
- 面板左侧有调整大小的手柄（8px 宽的区域）
- 拖动可以调整面板宽度（200px - 800px 范围）
- 支持鼠标和触摸屏操作

### 3. 双击编辑功能
- 在右侧备注面板中双击内容区域即可进入编辑模式
- 面板右上角提供编辑按钮（铅笔图标）
- 编辑模式下使用 Toast UI Editor 进行 Markdown 编辑

### 4. 编辑模式操作
- **进入编辑**：双击内容区域或点击编辑按钮
- **保存**：点击保存按钮（✓图标）或自动触发保存
- **取消**：点击取消按钮（×图标）
- **退出编辑**：保存或取消后自动返回只读模式

## 技术实现

### 修改的文件：

1. **Sidebar.vue** (`src/pages/Edit/components/Sidebar.vue`)
   - 添加 `sidebarWidth` 数据属性
   - 实现 `startResize`、`handleResize`、`stopResize` 方法
   - 添加调整大小手柄的样式

2. **NodeNoteSidebar.vue** (`src/pages/Edit/components/NodeNoteSidebar.vue`)
   - 添加 `isEditing` 状态控制
   - 实现 `initViewer`（只读）和 `initEditor`（编辑）方法
   - 添加 `startEdit`、`saveEdit`、`cancelEdit` 方法
   - 添加操作按钮（编辑、保存、取消）

3. **语言文件更新** (`src/lang/` 目录下各语言文件)
   - 在 `nodeNote` 对象中添加 `edit` 字段
   - 支持中文、英文、繁体中文、越南语

## 使用流程

1. **打开备注面板**：
   - 点击节点上的备注图标
   - 或使用工具栏的备注按钮

2. **查看备注**：
   - 备注内容显示在右侧面板中
   - 使用 Toast UI Viewer 渲染 Markdown

3. **调整面板大小**：
   - 将鼠标移到面板左侧边缘
   - 出现调整手柄时拖动即可

4. **编辑备注**：
   - 双击备注内容区域
   - 或点击面板右上角的编辑按钮（铅笔图标）
   - 使用 Markdown 编辑器进行编辑
   - 点击保存（✓）或取消（×）按钮

5. **关闭面板**：
   - 点击面板右上角的关闭按钮（×图标）
   - 或点击面板外部区域

## 注意事项

1. **编辑模式切换**：
   - 编辑时会保存原始内容，取消编辑可恢复
   - 保存后会自动触发思维导图数据更新

2. **面板状态**：
   - 面板宽度会保持，直到插件重新加载
   - 编辑状态在面板关闭时自动重置

3. **兼容性**：
   - 保持与原有浮窗备注功能的兼容
   - 原有 `NodeNote.vue` 对话框组件仍然可用

## 测试建议

1. **基本功能测试**：
   - 打开/关闭备注面板
   - 调整面板大小
   - 双击进入编辑模式
   - 保存和取消编辑

2. **边界情况测试**：
   - 空备注内容的处理
   - 长文本的显示和编辑
   - Markdown 格式的渲染
   - 图片等媒体内容的处理

3. **交互测试**：
   - 与思维导图其他功能的交互
   - 多语言切换
   - 主题切换（亮色/暗色）