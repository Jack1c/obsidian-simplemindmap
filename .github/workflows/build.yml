name: Build and Release Obsidian Plugin

on:
  push:
    branches: [ main, note ]
    paths:
      - 'plugin/**'
      - 'manifest.json'
      - '.github/workflows/build.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 检出代码
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: 设置 Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: 'plugin/package-lock.json'

    - name: 缓存依赖
      uses: actions/cache@v4
      with:
        path: |
          plugin/node_modules
          plugin/libs
        key: ${{ runner.os }}-npm-${{ hashFiles('plugin/package.json') }}
        restore-keys: |
          ${{ runner.os }}-npm-

    - name: 安装本地依赖
      working-directory: plugin
      run: |
        echo "检查本地依赖目录..."
        if [ ! -d "libs/mind-map/simple-mind-map" ]; then
          echo "本地依赖不存在，运行设置脚本..."
          npm run setup:local-deps
        else
          echo "本地依赖已存在，跳过设置"
        fi

    - name: 安装 Node.js 依赖
      working-directory: plugin
      run: npm ci

    - name: 构建插件
      working-directory: plugin
      env:
        NODE_ENV: production
      run: |
        npm run build
        echo "构建完成，检查输出文件..."
        ls -la ../main.js ../styles.css

    - name: 验证构建产物
      run: |
        if [ ! -f "../main.js" ]; then
          echo "❌ main.js 不存在，构建失败"
          exit 1
        fi
        if [ ! -f "../styles.css" ]; then
          echo "❌ styles.css 不存在，构建失败"
          exit 1
        fi
        echo "✅ 构建产物验证通过"
        echo "main.js 大小: $(wc -c < ../main.js) 字节"
        echo "styles.css 大小: $(wc -c < ../styles.css) 字节"

    - name: 创建 Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: v${{ github.run_number }}
        release_name: Release v${{ github.run_number }}
        body: |
          ## 自动构建版本

          - 构建时间: ${{ github.event.head_commit.timestamp }}
          - 提交: ${{ github.sha }}
          - 分支: ${{ github.ref }}

          ### 变更内容
          ${{ github.event.head_commit.message }}

          ### 使用方法
          1. 下载 main.js 和 styles.css
          2. 复制到 Obsidian 插件目录: `.obsidian/plugins/simple-mind-map/`
          3. 确保 manifest.json 也在同一目录
        draft: false
        prerelease: false

    - name: 上传 main.js
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ../main.js
        asset_name: main.js
        asset_content_type: application/javascript

    - name: 上传 styles.css
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ../styles.css
        asset_name: styles.css
        asset_content_type: text/css

    - name: 上传 manifest.json
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ../manifest.json
        asset_name: manifest.json
        asset_content_type: application/json

    - name: 构建成功通知
      if: success()
      run: |
        echo "✅ 构建成功！"
        echo "发布版本: v${{ github.run_number }}"
        echo "文件已上传到 Release"

    - name: 构建失败通知
      if: failure()
      run: |
        echo "❌ 构建失败"
        echo "请检查日志以获取详细信息"